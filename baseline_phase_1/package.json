{
  "name": "countwize-baseline-phase1b",
  "version": "1.0.0",
  "description": "Phase 1B Browser Evidence Capture for CountWize",
  "type": "module",
  "scripts": {
    "capture": "node baseline-capture.mjs --base https://countwize.com --out .",
    "capture:local": "node baseline-capture.mjs --base http://localhost:8080 --out .",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.8.0",
    "lighthouse": "^13.0.1",
    "playwright": "^1.40.0"
  }
}
